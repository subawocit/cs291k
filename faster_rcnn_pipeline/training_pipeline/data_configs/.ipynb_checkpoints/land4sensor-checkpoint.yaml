# Images and labels direcotry should be relative to train.py
TRAIN_DIR_IMAGES: 'data_land4sensor/train'
TRAIN_DIR_LABELS: 'data_land4sensor/train'
VALID_DIR_IMAGES: 'data_land4sensor/valid'
VALID_DIR_LABELS: 'data_land4sensor/valid'
TEST_DIR_IMAGES: 'data_land4sensor/test' 
TEST_DIR_LABELS: 'data_land4sensor/test' 

# Class names.
CLASSES: [
    '__background__',
    'landslides'
]

# Number of classes (object classes + 1 for background class in Faster RCNN).
NC: 2

# Whether to save the predictions of the validation set while training.
SAVE_VALID_PREDICTION_IMAGES: True
