# Images and labels direcotry should be relative to train.py
TRAIN_DIR_IMAGES: 'data_paper/train'
TRAIN_DIR_LABELS: 'data_paper/train'
VALID_DIR_IMAGES: 'data_paper/valid'
VALID_DIR_LABELS: 'data_paper/valid'
TEST_DIR_IMAGES: 'data_paper/test' 
TEST_DIR_LABELS: 'data_paper/test' 

# Class names.
CLASSES: [
    '__background__',
    'landslide'
]

# Number of classes (object classes + 1 for background class in Faster RCNN).
NC: 2

# Whether to save the predictions of the validation set while training.
SAVE_VALID_PREDICTION_IMAGES: True
